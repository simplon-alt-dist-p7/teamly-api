services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: teamly
    ports:
      - '5432:5432'
    volumes:
      - teamly_postgres_data:/var/lib/postgresql/data

volumes:
  teamly_postgres_data:
